#include <SDL.h>
#include <SDL_image.h>
#include "Game.h"

int main(int argc, char* args[])
{
	Game game;

	// have a loop, consisting of just game and end screen, if player presses q for quit, then the loop is terminated, if c then continue in loop and restart the game
	// stick main menu just before main game loop in game

	return 0;
}